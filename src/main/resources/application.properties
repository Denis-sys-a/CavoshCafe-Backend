# Datasource - MySQL (cavosh_db)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://mysql-24eb420e-denishilario3-b20d.k.aivencloud.com:15835/cavosh_db?sslMode=REQUIRED}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:PASSWORD_AIVEN_AQUI}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=86400000

server.port=8080

# Mail (SMTP - Gmail por defecto)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:giovannijara.dev@gmail.com}
spring.mail.password=${MAIL_PASSWORD:APP_PASSWORD_GMAIL_AQUI}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OTP - Verificacion de correo
app.otp.length=6
app.otp.expiration-minutes=10
app.otp.cooldown-seconds=60

# OTP - Recuperacion de contrasena (forgot / verify / reset password)
app.password-reset.otp-expiration-minutes=15
app.password-reset.token-expiration-minutes=10
app.password-reset.cooldown-seconds=60